import ProjectDescription

// Root Tuist configuration.
// If your installed Tuist version uses a different config API, adjust here
// (older versions use `Tuist/Config.swift` with `let config = Config(...)`).
let tuist = Tuist()
