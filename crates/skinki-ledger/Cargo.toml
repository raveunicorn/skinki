[package]
name = "skinki-ledger"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Derivation ledger — staleness-aware memory via hash-linked reasoning chains"

[dependencies]
serde.workspace = true
serde_json.workspace = true
