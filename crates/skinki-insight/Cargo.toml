[package]
name = "skinki-insight"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Stage 5 — Insight Engine: deterministic discovery + statistical validation + cite-or-silence"

[dependencies]
serde.workspace = true
serde_json.workspace = true
skinki-corpus.workspace = true
skinki-eval.workspace = true
# Justification: Stage 5 T5 — ledger-wire surfaced insights so a changed premise
# flags the insight stale. The derivation ledger is the only source of staleness
# propagation in the system; every insight MUST carry a derivation record.
skinki-ledger.workspace = true
